let hero:string; // when you declare a varible for future it becomes any so it will behave like normal varible in javascript so please tell varible that what type of type it is going to store.

function getHello() {
  return "Hello";
}
hero = getHello();
console.log(hero)
export{};
